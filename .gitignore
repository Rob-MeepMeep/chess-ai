# Virtual environment — never commit this, it's huge and machine-specific
venv/

# Python bytecode cache
__pycache__/
*.pyc
*.pyo

# Model checkpoints and training data (will be large files)
checkpoints/
logs/
*.pt
*.pth
replay_buffer/

# TensorBoard logs
runs/

# OS files
.DS_Store

# Project context — local only, never upload
CLAUDE.md
